{
  "name": "@theatrical/landing",
  "version": "0.1.0",
  "private": true,
  "description": "Landing page for the Theatrical developer platform",
  "scripts": {
    "dev": "npx serve public",
    "build": "echo 'Static site — no build step required'"
  }
}
